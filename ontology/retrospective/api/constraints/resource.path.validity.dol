constraint resource.path.validity {
  uri matches orchestrator_scheme
  path never malforms
}

exegesis {
  Resource path constraint validates MCP resource URIs in resources/mod.rs.
  parse_resource_uri() requires "orchestrator:
  nodes, nodes/<id>, workloads, workloads/<id>, cluster/status, cluster/metrics.
  Returns Option<ResourcePath> enum. Invalid URIs return None, causing
  JsonRpcResponse::error with INVALID_PARAMS code. Ensures resource access follows
  defined schema.
}
