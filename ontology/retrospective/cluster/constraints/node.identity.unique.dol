constraint node.identity.unique {
  id matches uuid.v4
  id never duplicates
}

exegesis {
  Enforces unique node identification.

  From the code:
  - NodeId is type alias for uuid::Uuid
  - Default config generates uuid::Uuid::new_v4()
  - parse_node_id() validates UUID format
  - ChitchatId enforces uniqueness via node_id string
  - Generation ID distinguishes node restarts
}
